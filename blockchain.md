## THINGS NEED TO DO

- One-page technical whitepaper: problem → data flywheel → token economy → roadmap
- A clear token utility diagram: contribute data → earn token → spend to use model
- Testnet token contract (pick ONE chain first, port later)
- Simple contribution tracking: wallet address linked to userId, log contributions, calculate rewards
